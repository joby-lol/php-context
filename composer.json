{
    "name": "joby/context",
    "description": "A lightweight, static dependency injection container for PHP that combines simplicity with powerful features like automatic dependency resolution, callable execution with injected dependencies, and file inclusion with docblock-based dependency injection.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "~8.2",
        "psr/simple-cache": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Joby\\ContextInjection\\": "src/"
        },
        "files": [
            "functions.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Joby\\ContextInjection\\": "tests/"
        }
    },
    "authors": [
        {
            "name": "Joby Elliott",
            "email": "code@joby.lol"
        }
    ],
    "require-dev": {
        "phpunit/phpunit": "^12.1",
        "phpstan/phpstan": "^2.1"
    }
}
